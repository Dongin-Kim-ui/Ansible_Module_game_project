apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  namespace: game-server
spec:
  selector:
    app: {{ app_name }}
  type: NodePort
  ports:
    - protocol: TCP
      port: {{ container_port }}
      targetPort: {{ container_port }}
      nodePort: {{ node_port }}
